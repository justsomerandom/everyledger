# Ledger Model

This document will define the ledger model, including asset types, accounts, transactions, immutable entries, double-entry validation, balances, snapshots, exchanges, and reconciliation semantics.
